import React from 'react';
import {Text} from "react-native";

const NoData = () => {
  return (
    <Text>
      Unfortunately we have no data to display :(
    </Text>
  );
};

export default NoData;
